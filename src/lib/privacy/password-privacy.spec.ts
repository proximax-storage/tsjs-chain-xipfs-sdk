// import { expect } from 'chai';
import 'mocha';
// import { PasswordPrivacyStrategy } from './password-privacy';
/*
describe('PasswordPrivacy', () => {
  it('should encrypt the message using password strategy', () => {
    const password =
      'lkNzBmYmYyNTExZjZmNDYyZTdjYWJmNmY1MjJiYjFmZTk3Zjg2NDA5ZDlhOD';
    const plainText =
      'ProximaX is an advanced extension of the Blockchain and Distributed Ledger Technology (DLT) with utility-rich services and protocols';

    const passwordPrivacy = new PasswordPrivacyStrategy(password);
    const encryption = passwordPrivacy.encrypt(plainText);
    encryption.subscribe(encrypted => {
      console.log('Encrypted data ' + encrypted);
      expect(encrypted).to.be.not.equal(plainText);
    });
  });
});
*/
