import { NetworkType } from 'nem2-sdk';

export enum BlockchainNetworkType {
  MIJIN_MAIN = NetworkType.MIJIN,
  MIJIN_TEST = NetworkType.MIJIN_TEST,
  MAIN_NET = NetworkType.MAIN_NET,
  TEST_NET = NetworkType.TEST_NET
}
